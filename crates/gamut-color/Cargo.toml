[package]
name = "gamut-color"
description = "Colour science on the CPU: matrices, transfer functions, tone mapping, LUTs and ICC"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
log = { workspace = true }
moxcms = { workspace = true }
gamut-core = { path = "../gamut-core" }
