[package]
name = "slate-core"
description = "Project document: photo edit parameters, timeline model, presets and undo history"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
