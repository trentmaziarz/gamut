[package]
name = "gamut-templates"
description = "Template format: an SVG plus a TOML sidecar naming text slots, fonts, duration and keyframes"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
